# API

FastAPI backend scaffold for the saju product.

## Run
- Install dependencies with `pip install -e .[dev]`
- Start the server with `uvicorn app.main:app --reload`
- Open `http://localhost:8000/docs`

